package com.leafclient.buddy;

import com.leafclient.buddy.manager.EventManager;

/**
 * Marks an object as a {@link Event}. Every object posted to the {@link EventManager}
 * needs to be marked by this interface.
 */
public interface Event {
}
